<?php
if (!isset($DROP_INDEX)) {
    die("This page cannot be accessed directly.");
}

# Root URL for this Drop installation, e.g. https://example.com/drop"
$ROOT_URL = "https://example.com";

$DB_SERVER = "127.0.0.1";
$DB_USER = "example_user";
$DB_PASSWORD = "example_password";
$DB_DBNAME = "drop_database";
$DB_TABLE_PREFIX = "";

$SHOW_ERROR_DETAILS = true;
$UPLOAD_DIR = "uploads";
?>
